name: CI

on:
  push:
    tags:
    - "v*.*.*"

env:
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

jobs:
  test:
    uses: ./.github/workflows/test.yaml

  release:
    uses: ./.github/workflows/release.yaml
